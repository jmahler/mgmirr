name: Tests
on: [push, pull_request]

jobs:
  main:
    name: Main
    runs-on: ubuntu-18.04
    env:
      GOPATH: "${{ github.workspace }}/go:/usr/share/gocode"
      LD_LIBRARY_PATH: "${{ github.workspace }}/go/src/github.com/libgit2/git2go/dynamic-build/install/lib/"
      PKG_CONFIG_PATH: "${{ github.workspace }}/go/src/github.com/libgit2/git2go/dynamic-build/build/"
    steps:
    - name: Setup Go
      uses: actions/setup-go@v1
      with:
        go-version: 1.10
    - name: Check out code into the Go module directory
      uses: actions/checkout@v2
      with:
        fetch-depth: 1
        path: go/src/github.com/jmahler/rpmmirr
    - name: Checkout git2go
      run: |
        go get -d github.com/libgit2/git2go
    - name: Install Packages
      run: |
        sudo apt install cmake libssh2-1-dev
    - name: Setup git2go (dynamic)
      working-directory: "${{ github.workspace }}/go/src/github.com/libgit2/git2go"
      run: |
        git checkout master
        git submodule update --init
        make test-dynamic
        make install-dynamic
    - name: Build
      working-directory: "${{ github.workspace }}/go/src/github.com/jmahler/rpmmirr"
      run: |
        go build -v
    - name: Run go fmt
      working-directory: "${{ github.workspace }}/go/src/github.com/jmahler/rpmmirr"
      run: |
        DIFF="$(gofmt -d -s .)"
        test -z "${DIFF}" || (echo "${DIFF}" && exit 1)
    - name: Test
      run: |
        go test -v github.com/jmahler/rpmmirr -coverprofile=profile.cov
    - name: Send Coverage
      uses: shogo82148/actions-goveralls@v1
      with:
        path-to-profile: profile.cov
        github-token: ${{ secrets.GITHUB_TOKEN }}
